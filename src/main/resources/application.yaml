spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/heltfit
    username: heltfit
    password: heltfit
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
      show-sql: true